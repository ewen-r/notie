import React from "react";

/** Component to display Header. */
function Header() {
  const appTitle = 'Keeper';

  return (
    <header>
      <h1>{appTitle}</h1>
    </header>
  );
}


export default Header;
